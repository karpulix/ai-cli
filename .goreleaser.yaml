version: 2

project_name: ai-cli

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X=github.com/karpulix/ai-cli/internal/version.Version={{.Version}}
      - -X=github.com/karpulix/ai-cli/internal/version.Commit={{.Commit}}
      - -X=github.com/karpulix/ai-cli/internal/version.Date={{.Date}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

brews:
  - name: ai-cli
    repository:
      owner: karpulix
      name: homebrew-tools
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/karpulix/ai-cli"
    description: "TUI assistant for shell commands via LLM"
    install: |
      bin.install "ai-cli"
    test: |
      system "#{bin}/ai-cli", "--version"
    commit_author:
      name: github-actions[bot]
      email: 41898282+github-actions[bot]@users.noreply.github.com
